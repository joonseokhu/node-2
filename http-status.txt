http status

엔드포인트가 없을때, /fooo/bar/bazzzzz 이상한 엔드포인트 - 400 (bad request)
특정 아이디로 엔티티 조회할때 없을때, /product/1245 가 없는거랑 - 404 (not found)

422 ---> req.params.id 이 (mysql)숫자이기를, (mongodb)ObjectID이기를 기대했는데, 엉뚱한 형식이 들어왔다.

401 ---> 암호를 요구하는 상황, 또는 인증수단을 요구하는 상황에서, 그게 틀림
403 ---> 권한이 없어서 거부, 또는 시스템상 거부 (제한시간이나 갯수제한, 품절)

비공개 처리된 엔티티, 삭제된 엔티티, 접근이 제한된 엔티티와 삭제된 엔티티와, 처음부터 존재하지 않았던 엔티티
----> 404 (있는지 없는지도 몰라야함)

목록을 요청할때, 아무것도 조회를 못했으면, 200에 빈배열